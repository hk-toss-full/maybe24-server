spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        # example
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/auth/**
        # add your service
eureka:
  instance:
    prefer-ip-address: true
