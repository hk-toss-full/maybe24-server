spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        # example
        - id: product-service
          uri: lb://PRODUCT
          predicates:
            - Path=/product/**
            - Method=POST
        # add your service
#      discovery:
#        locator:
#          enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
